version: '3.8'

services:
  copilot:
    build: .
    container_name: ai-inbox-copilot
    ports:
      - "8501:8501"
    volumes:
      - .:/app
    environment:
      - GROQ_API_KEY=${GROQ_API_KEY}
    restart: unless-stopped
